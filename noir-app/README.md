# Noir React App

This folder contains the source code for the React + Tailwind CSS website for the luxury water brand **Noir**. It was bootstrapped with Vite.

## Development

```bash
npm install
npm run dev
```

## Production Build

Run `npm run build` to generate the static site under `dist/`. Deploy the `dist` folder to Cloudflare Pages or any static host.
